spring:
  application:
    name: solo-leveling-ai
  kafka:
    bootstrap-servers: ${SERVER_HOST}:9092
  ai:
    openai:
      api-key: ${CHAT_TOKEN}
      base-url: https://api.deepseek.com
      chat:
        options:
          model: deepseek-chat